{
  "name": "qrgen",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build --target=bun --production --sourcemap --outdir=out ./src/index.ts",
    "dev": "bun run --hot ./src/index.ts",
    "lint": "oxlint",
    "fmt": "oxfmt"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "oxfmt": "^0.66.0",
    "oxlint": "^1.81.0",
    "uqr": "^0.1.3"
  }
}
